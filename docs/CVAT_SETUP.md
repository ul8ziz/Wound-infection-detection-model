# CVAT setup (moved)

Documentation for self-hosted CVAT now lives in the repository under **[`cvat/CVAT_SETUP.md`](../cvat/CVAT_SETUP.md)**.

Run the helper from the repository root:

```bash
python cvat/setup_cvat.py --only-folders
```
